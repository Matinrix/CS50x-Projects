/*this code is a simple code which ive just coded it
    and its a sipmle linear algorithm whichis searching the arrays and tell us
        if the users entered number exist in the array or not!
*/
int get_int(char *text);
#include <stdio.h>
int main (void)
{

}

int get_int(char *text)
{
    int num;
    printf("%s", text);
    scanf("%i", &num);
    return num;
}
